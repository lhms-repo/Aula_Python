Execute os comandos abaixo para disponibilizar o site:

```bash
cd <diretório do repositório>\Aula_Python\projetos\webapp
pip install flask
python -c "import flask; print(flask.__version__)"
flask run
```